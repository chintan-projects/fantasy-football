# What is done, what is next. Claude reads this at session start. Keep it under 50 lines.
# Completed milestones and closed questions live in PROGRESS-archive.yaml.
project: fantasy-football-copilot
updated: 2026-09-13

milestones:
  - id: M0-M4
    name: Setup, Yahoo read path, projections, lineup, waivers, MCP front door
    status: done
    notes: See PROGRESS-archive.yaml. 347 tests passing; make check and make test green.

  - id: M5
    name: Write path
    status: done_as_assisted
    notes: >
      Closed 2026-09-14, not blocked. Yahoo's access page states write access is not
      available to anyone, so there is no approval to wait for. AssistedExecutor is the
      shipping path -- full decision, exact move, deep link, owner taps it -- and its
      output now names players and dollars rather than player keys. YahooApiExecutor stays
      in the tree, unproven; scripts/probe_write.py is a monitor, not a gate.
      Action outstanding: set FF_WRITE_EXECUTOR=assisted on the deployed app. It is still
      dryrun, which sends nothing but also tells the owner nothing actionable.

  - id: M6
    name: Calibration
    status: in_progress
    notes: >
      Built: the nflverse actuals adapter, domain/calibration.py (paired MAE per source
      with a significance test, lineup hindsight against the highest-projection lineup),
      services/calibration.py, the ff_how_am_i_doing tool, and a Tuesday 06:00 ingest job.
      Two defects came out of it -- BUG-012, per-source projections were being averaged
      away so the headline metric was uncomputable, and BUG-013, two persisted shapes for
      one recommendation.
      Not built: a backtest that replays stored snapshots, and hit rate on individual
      start/sit calls (contested_slots is returned to the owner but not persisted, so the
      pairs cannot be recovered).

in_progress: []

next:
  - Persist contested_slots with each lineup recommendation, so the per-decision hit rate
    becomes computable. Same class of loss as BUG-012 -- not recoverable after the fact.
  - Re-record tests/fixtures/yahoo/ against the live league once Yahoo approves access.
    Every Yahoo fixture is hand-built from documented shapes today.
  - Fix BUG-001, two mypy errors in adapters/yahoo/executors.py. Now live code rather
    than dormant code, since the write path is deployed.

open_questions:
  - id: yahoo-api-access
    question: Will Yahoo approve the Fantasy Sports API read application?
    status: blocked, acknowledged 2026-09-13, quoted 1-2 weeks
    impact: >
      OAuth works end to end and the token stores cleanly; every Fantasy endpoint then
      answers 401 oauth_problem="additional_authorization_required". Re-confirmed
      2026-09-13. So the parsers are untested against real payloads, M5 cannot start, and
      the weekly snapshot jobs have nothing to snapshot -- which means calibration has
      nothing to score until this clears, however finished the calibration code is. See
      BUG-004 for the scope fix that was needed to get this far.
    next_step: >
      Application submitted 2026-09-14 via sports.yahoo.com/developer/access. Also confirm
      Fantasy Sports is ticked under API Permissions at developer.yahoo.com/apps -- an
      unticked permission gives the identical error and is a 30-second fix independent of
      the review. Then re-run make leagues and re-record the fixtures.
      Read access is the whole question now; write is settled and not coming (CLAUDE.md 6).

# Backlog lives in PROGRESS-archive.yaml. This file is active work and blockers only.
