# What is done, what is next. Claude reads this at session start. Keep it under 50 lines.
# Completed milestones and closed questions live in PROGRESS-archive.yaml.
project: fantasy-football-copilot
updated: 2026-09-13

milestones:
  - id: M0-M4
    name: Setup, Yahoo read path, projections, lineup, waivers, MCP front door
    status: done
    notes: See PROGRESS-archive.yaml. 347 tests passing; make check and make test green.

  - id: M5
    name: Write path
    status: blocked
    blocked_by: [yahoo-api-access]
    notes: >
      Two-step approval flow, journal and dry-run executor are built and tested through
      ff_propose_claim / ff_confirm. What is unproven is YahooApiExecutor: nothing has
      ever been written to Yahoo. make probe settles it once the app is approved.

  - id: M6
    name: Calibration
    status: not_started
    notes: >
      The part that makes the rest trustworthy. Snapshots and recommendations are already
      being persisted by the tools and the weekly jobs, so the inputs exist. What is
      missing is the scoring: MAE per source per week, hit rate against the lineup that
      was already set, and a simulated season record.

in_progress: []

next:
  - Deploy to Fly and connect it as a custom connector. docs/DEPLOY.md is written; nothing
    has been deployed. The GitHub OAuth app does not exist yet.
  - Re-record tests/fixtures/yahoo/ against the live league once Yahoo approves access.
    Every Yahoo fixture is hand-built from documented shapes today.

open_questions:
  - id: yahoo-api-access
    question: Will Yahoo approve the Fantasy Sports API application?
    status: blocked, acknowledged 2026-09-13, quoted 1-2 weeks
    impact: >
      OAuth works end to end and the token stores cleanly; every Fantasy endpoint then
      answers 401 oauth_problem="additional_authorization_required". So the parsers are
      untested against real payloads and M5 cannot start. See BUG-004 for the scope fix
      that was needed to get this far.
    next_step: >
      Confirm Fantasy Sports is ticked under API Permissions at developer.yahoo.com/apps
      -- an unticked permission gives the identical error and is a 30-second fix. Then
      wait, re-run make leagues, and re-record the fixtures.

backlog:
  - id: losing-faab-bids
    what: Capture losing FAAB bids from the Yahoo desktop UI
    why: >
      KNOWN-001. The API publishes winning bids only, so the opponent model learns the
      clearing price and nothing about the bid distribution -- credible_bidders is an
      upper bound, which shades less and bids high. The losing bids are visible in the
      desktop transaction detail view, so a scraper could capture them.
    cost: A scraper against an unversioned HTML page, re-broken on every redesign. Not
      worth starting before M6 can show the bids are actually wrong.

  - id: nfl-schedule-source
    what: An NFL schedule source, so PlayerContext can carry opponent_team
    why: >
      Without it only same-team correlation applies in the simulation. The margin
      distribution comes out slightly wider than reality, which pulls win probability
      toward 50% -- less decisive, not more. Stated in services/week.py. [theory]

  - id: frontend
    what: The Next.js frontend, demoted 2026-09-13
    why: >
      Kept, not deleted. It is not built, tested or deployed and it is not kept in step
      with the payload shape. See frontend/README.md and docs/DECISIONS.md.
